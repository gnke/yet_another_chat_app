!!!
html
    head
        title= "Simple web chat"
        script(src='/chat.js')
        script(src='/socket.io/socket.io.js')
    body
        #content(id='messages', style='width: 500px; height: 300px; margin: 0 0 20px 0; border: solid 1px #999; overflow-y: scroll;')
        .controls
            input.field(id='message', style='width:350px;')
            input.send(id='send', type='button', value='send')